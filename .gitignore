PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=webgis_db
PGUSER=postgres
PGPASSWORD=DbGIS123

# allowed web origins (Netlify + local dev)
ALLOWED_ORIGINS=http://localhost:5500,https://dulcet-lolly-53b12c.netlify.app

# API server port
PORT=3000

# whitelist tables or views to expose
WHITELIST=public.blocks
